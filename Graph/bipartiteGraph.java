package Graph;

// Bipartite graph -> color all nodes such that no two adj nodes have same color
// cycle with odd length case cannot be colored else it can form bipartite
public class bipartiteGraph {
    
}
